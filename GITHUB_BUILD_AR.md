# بناء TM SAT APK بدون Android Studio

هذا المشروع مجهز بحيث **GitHub يبني ملف APK على سيرفراته**؛ جهازك لا يحتاج Android Studio.

## أول مرة فقط

1. افتح GitHub وسجل الدخول.
2. أنشئ Repository جديد باسم `TM-SAT`، واجعله **Private** إذا لا تريد أن يظهر المشروع للناس.
3. فك ضغط هذا الملف عندك.
4. داخل المجلد ستجد ملفات مثل `app` و `.github` و `build.gradle.kts`.
5. ارفع **محتويات المجلد** إلى الـ Repository، وليس ملف ZIP نفسه فقط.

## استخراج APK

بعد اكتمال الرفع:

1. افتح تبويب **Actions** في GitHub.
2. اختر **Build TM SAT APK**.
3. اضغط **Run workflow** ثم **Run workflow** مرة ثانية.
4. انتظر إلى أن تصبح العملية بعلامة صح خضراء.
5. افتح نتيجة التشغيل، وانزل إلى قسم **Artifacts**.
6. اضغط **TM-SAT-APK**.
7. سيُنزّل ملف ZIP؛ افتحه وستجد بداخله `TM-SAT.apk`.

## ملاحظات

- الـ APK الناتج هو **Debug APK** وموقّع تلقائياً بمفتاح Debug، وهو مناسب للتجربة والتثبيت المباشر على Android Box.
- لا يحتاج الكمبيوتر لديك إلى Android Studio أو Android SDK لأن البناء يتم على GitHub.
- عند كل تعديل ورفع جديد للمشروع، GitHub سيعيد بناء APK تلقائياً.
- استخدم القنوات والمحتوى الذي تملكه أو لديك ترخيص ببثه فقط.
